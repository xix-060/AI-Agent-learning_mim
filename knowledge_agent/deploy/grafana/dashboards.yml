# Grafana 数据源自动预置（挂载到 /etc/grafana/provisioning/datasources/）
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
