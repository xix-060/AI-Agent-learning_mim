# Grafana 数据源自动预置（挂载到 /etc/grafana/provisioning/datasources/）
# 后续可在 grafana/dashboards/ 下加 dashboard JSON 并配 providers 自动加载
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
